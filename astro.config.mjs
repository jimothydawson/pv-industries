import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  // No specific integrations or output modes needed for this basic setup yet.
  // site: 'https://example.com', // Optional: Replace with your actual site URL if deploying
});