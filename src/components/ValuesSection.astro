---
// Values section component
const sectionData = {
  subtitle: "04",
  title: "The values that drive everything we do",
  description: "",
  values: [
    {
      icon: "/images/quality-icon-industrial-x-webflow-template.svg",
      title: "Leadership",
      description: "PV Industries are leaders in solar panel waste management and we aim to shape photovoltaic (PV) recycling in Australia. Through strong leadership we believe we can deliver real action by educating society and demonstrating the benefits of solar panel reuse and recycling."
    },
    {
      icon: "/images/innovation-icon-industrial-x-webflow-template.svg",
      title: "Integrity",
      description: "Our business grew from strong moral principles in an attempt to reduce Australia's environmental footprint, so integrity is at the core of our business. Our high level of service is founded in honesty which allows us to build trust with our partners and clients."
    },
    {
      icon: "/images/security-icon-industrial-x-webflow-template.svg",
      title: "Accountability",
      description: "Many Australians are sceptical of recycling, misinformed and lack an understanding of how their waste is managed and recycled. We aim to change this, being held accountable by our customers as they deserve to know what happens to the waste they recycle."
    }
  ]
};
---

<section id="values" class="values-section">
  <div class="container">
    <!-- Section Header -->
    <div class="section-header">
      <div class="header-content">
        <div class="subtitle-wrapper">
          <div class="subtitle-divider left"></div>
          <div class="subtitle">{sectionData.subtitle}</div>
          <div class="subtitle-divider"></div>
        </div>
        
        <div class="title-wrapper">
          <h2 class="section-title">{sectionData.title}</h2>
        </div>
        
        {sectionData.description && (
          <div class="section-description">
            <p>{sectionData.description}</p>
          </div>
        )}
      </div>
    </div>

    <!-- Values Grid -->
    <div class="values-grid">
      {sectionData.values.map((value, index) => (
        <div class="value-card" style={`animation-delay: ${index * 0.1}s`}>
          <div class="value-icon">
            <img src={value.icon} alt={`${value.title} Icon`} />
          </div>
          
          <div class="value-content">
            <h3 class="value-title">{value.title}</h3>
            <p class="value-description">{value.description}</p>
          </div>
        </div>
      ))}
    </div>
  </div>
</section>

<style>
  .values-section {
    padding: var(--space-20) 0;
    background: var(--color-neutral-200);
    overflow: hidden;
  }

  /* Section Header */
  .section-header {
    margin-bottom: var(--space-12);
    max-width: 612px;
    margin-left: auto;
    margin-right: auto;
    text-align: center;
  }

  @media (max-width: 767px) {
    .section-header {
      max-width: 550px;
    }
  }

  .header-content {
    opacity: 0;
    transform: translateY(30px);
    animation: fadeInUp 0.6s ease-out forwards;
  }

  .subtitle-wrapper {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: var(--space-4);
    margin-bottom: var(--space-6);
  }

  .subtitle {
    font-size: var(--font-size-lg);
    font-weight: 600;
    color: var(--color-primary);
    flex-shrink: 0;
  }

  .subtitle-divider {
    height: 2px;
    background: var(--color-primary);
    flex: 1;
    max-width: 60px;
  }

  .subtitle-divider.left {
    display: block;
  }

  .title-wrapper {
    max-width: 500px;
    margin: 0 auto var(--space-4);
  }

  @media (max-width: 767px) {
    .title-wrapper {
      max-width: none;
    }
  }

  .section-title {
    font-size: clamp(2rem, 4vw, 2.5rem);
    font-weight: 600;
    color: var(--color-neutral-900);
    line-height: 1.2;
    margin: 0;
  }

  .section-description p {
    color: var(--color-neutral-600);
    font-size: var(--font-size-base);
    line-height: 1.6;
    margin: 0;
  }

  /* Values Grid */
  .values-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: var(--space-8);
    position: relative;
    opacity: 0;
    transform: translateY(30px);
    animation: fadeInUp 0.8s ease-out 0.3s forwards;
  }

  @media (max-width: 767px) {
    .values-grid {
      grid-template-columns: 1fr;
      gap: var(--space-6);
    }
  }

  /* Value Cards */
  .value-card {
    background: var(--color-neutral-100);
    border-radius: var(--radius-xl);
    padding: var(--space-8);
    box-shadow: var(--shadow-md);
    transition: all var(--transition-normal);
    opacity: 0;
    transform: translateY(30px);
    animation: fadeInUp 0.6s ease-out forwards;
  }

  .value-card:hover {
    transform: translateY(-4px);
    box-shadow: var(--shadow-lg);
  }

  .value-icon {
    margin-bottom: var(--space-6);
  }

  .value-icon img {
    width: 48px;
    height: 48px;
    display: block;
  }

  .value-content {
    /* Content styling */
  }

  .value-title {
    font-size: var(--font-size-xl);
    font-weight: 600;
    color: var(--color-neutral-900);
    line-height: 1.3;
    margin: 0 0 var(--space-3);
  }

  .value-description {
    color: var(--color-neutral-600);
    font-size: var(--font-size-base);
    line-height: 1.6;
    margin: 0;
  }

  /* Grid positioning classes for specific layouts */
  .value-card:nth-child(1) {
    /* Top left */
  }

  .value-card:nth-child(2) {
    /* Top center */
  }

  .value-card:nth-child(3) {
    /* Top right */
  }

  /* Responsive adjustments */
  @media (max-width: 767px) {
    .values-section {
      padding: var(--space-16) 0;
    }

    .section-header {
      margin-bottom: var(--space-10);
    }

    .value-card {
      padding: var(--space-6);
    }

    .value-icon {
      margin-bottom: var(--space-5);
    }

    .value-icon img {
      width: 40px;
      height: 40px;
    }

    .value-title {
      font-size: var(--font-size-lg);
    }

    .value-description {
      font-size: var(--font-size-sm);
    }
  }

  /* Animation preferences */
  @media (prefers-reduced-motion: reduce) {
    .header-content,
    .values-grid,
    .value-card {
      opacity: 1;
      transform: none;
      animation: none;
    }

    .value-card:hover {
      transform: none;
    }
  }
</style>